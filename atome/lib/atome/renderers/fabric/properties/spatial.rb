module FabricProperty
  # def x_fabric(canvas_id, obj_id, params)
    def x_fabric(params)
    # `fabric_prop(#{canvas_id},#{obj_id},#{{ left: params[:x] }})`
  end

  # def y_fabric(canvas_id, obj_id, params)
    def y_fabric( params)
    # `fabric_prop(#{canvas_id},#{obj_id},#{{ top: params[:y] }})`
    end

  def z_fabric( params)
  end
end