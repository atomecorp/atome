module SpatialHtmlRenderer
  def self.render_x(atome_id, params)
    "#{atome_id} #{params}"
  end

  def self.render_xx(atome_id, params)
    "#{atome_id} #{params}"
  end

  def self.render_y(atome_id, params)
    "#{atome_id} #{params}"
  end

  def self.render_yy(atome_id, params)
    "#{atome_id} #{params}"
  end

  def self.render_z(atome_id, params)
    "#{atome_id} #{params}"
  end
end